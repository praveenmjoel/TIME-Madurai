name: Daily AI Coaching

on:
  schedule:
    # Primary:  1:30 AM UTC = 7:00 AM IST
    - cron: '30 1 * * *'
    # Backup 1: 2:30 AM UTC = 8:00 AM IST  (fires if GitHub missed the 7 AM slot)
    - cron: '30 2 * * *'
    # Backup 2: 3:30 AM UTC = 9:00 AM IST  (final safety net)
    - cron: '30 3 * * *'
  workflow_dispatch:
    # Allow manual trigger from GitHub Actions UI

jobs:
  coach:
    name: Generate and Post Daily Coaching
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: scripts/package.json

      - name: Install dependencies
        working-directory: scripts
        run: npm install

      - name: Run daily coaching script
        working-directory: scripts
        env:
          FIREBASE_SERVICE_ACCOUNT_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          CLICKUP_API_KEY: ${{ secrets.CLICKUP_API_KEY }}
        run: node daily-coaching.js
